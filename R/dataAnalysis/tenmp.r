heisenberg <- read.csv(file="linksPerUser.csv",head=TRUE,sep=",")
heisenberg[2]

plot(data.matrix(heisenberg), type="o",names.arg=c(1:20))
axis(1, at=1:20, lab=c(1:20))

class(res[1,1])
tmp=as.character(res[1,1])
class(tmp)

foi (i in 1:10){
	ime <- "Ime"+i
	ime
}

ime <- "ime"
ime
ime1 <-ime.i
ime1